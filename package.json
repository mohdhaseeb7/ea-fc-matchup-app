{
  "name": "ea-fc-matchup-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "npm --prefix frontend run start"
  }
}
